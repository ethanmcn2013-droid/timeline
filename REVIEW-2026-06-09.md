_Director of Roadmap Product Excellence — Layer 2 (Recommend)_

# Roadmap Excellence Review · 2026-06-09

Planning should feel clear and calm. The mandate is one plain-English page anyone can open. No account, no jargon. Built for the people who never log in. The landing-page gesture is a track being laid.

Read pass. No writes. Read-without-login path tested against `src/app/[workspaceSlug]/page.tsx`, the marketing homepage at `src/app/page.tsx`, the refusals surface at `src/app/[workspaceSlug]/refusals/page.tsx`, and the shared update at `src/app/[workspaceSlug]/update/page.tsx`. Recent commits inspected back to `2e7543b` (2026-06-03).

The product is closer to the mandate than it was a month ago. The cuts below are the things still standing between a wedding planner's mother and the page she was sent.

---

## The five gaps, in order of cost to the non-logged-in reader

### Gap 1 · The hero is four numbers and a fifth chrome strip before the first item

**Surface.** `src/app/[workspaceSlug]/page.tsx` lines 590–740. The public reader lands on, in this order: demo-banner strip (if demo) → 12px sticky `WorkspaceHeader` → `MetaStrip` (uppercase mono row: name · date-range · weeks · milestones) → `<h1>` → description → "Shared by … Last updated …" → `WorkspaceViewSwitcher` (four tabs) → `BigStat` row (Total · Done · Doing · Next · Waiting · Won't do).

**Friction.** Time-to-first-item: the reader has crossed seven distinct chrome blocks before reading a single thing the work is actually about. Count the rows above the fold for the non-logged-in mother-of-the-bride: she gets `MetaStrip` (uppercase mono, decorative), then the title, then a four-tab view switcher she did not ask for, then a row of six tabular-num counts. The dashboard energy this is meant to refuse has been reassembled, calmer, in the hero.

The `BigStat` row in particular betrays the mandate. The brief is "tell me what matters today, in words I would use." Total · Done · Doing · Next · Waiting · Won't do is the language of a project manager looking at their own grid. The reader was sent a link to see what is happening, not to audit lane balances.

**The cut.**
- Remove the `BigStat` band from the public Overview hero. Keep it for the owner view only (gate on `isOwner`, same pattern already used at line 703 for `Needs attention`).
- Demote the `MetaStrip` (lines 594–605). The H1 + the one-line "Shared by X · Last updated Y" already carry identity, attribution, recency. The uppercase mono row of date-range · weeks · milestone-count is decoration that costs the reader a full visual sweep before the title lands.
- Defer the `WorkspaceViewSwitcher` until after the first list paints. Four tabs at the top of a page someone was *sent* implies a choice the recipient did not come to make. Overview is the right read-default; the switcher belongs as a quiet inline control above the item list, not in the title block.

**Layer.** 2 (Recommend). Cut the BigStat band from public; demote MetaStrip; move the view switcher below the fold. Bring this to Brand if MetaStrip is considered load-bearing across surfaces — but the surface this serves is the owner-aware editor, not the read-without-login page.

---

### Gap 2 · Four views is three views the reader did not ask for

**Surface.** `src/components/roadmap/workspace-view-switcher.tsx` lines 8–13. `VIEWS = [overview, roadmap, milestones, schedule]`. Four parallel renderings of the same data, all SSR'd into the same response (`src/app/[workspaceSlug]/page.tsx` lines 826–878 render all four in parallel as Suspense children).

**Friction.** The mandate is one page. The product ships four. For the non-logged-in reader this manifests as: a four-tab pill at the top of the hero, an inline `<script>` block that pre-paints `data-view` so deep-links don't flash (page.tsx lines 137–162), and four full panel bodies streamed into the SSR HTML (line 771 `WorkspaceViewBodyStatic`). The complexity of the page is dictated by views the recipient did not navigate to.

This is also a payload cost the calm-pages mandate should refuse. Every public link ships four serialized views — `RoadmapFlow`, `ScheduleView`, `MilestoneMap`, and the Overview tree — into a single HTML response. ISR caches it, but the byte cost lands on every cold reader.

**The cut.**
- Ship one view for the read-without-login reader: Overview. Make Roadmap / Milestones / Schedule owner-only, gated on `isOwner` in the page component (the gate already exists for Needs-attention at line 703).
- Drop the view switcher from the public header entirely. The recipient does not pick a perspective; the owner picks the perspective they want to share.
- If the four-view kit is brand-load-bearing for editor demos, keep it behind the auth gate. The public URL pays for itself in clarity, not optionality.

**Layer.** 3 (Founder-gate). This is a material feature reduction on a surface the operator has invested in over multiple cycles (R·20–R·22 hardened the multi-view rig). Brief to Ethan. The recommendation: the discipline of "one page anyone can open" earns more brand than four views compounding on each other.

---

### Gap 3 · `KIND` pills are an internal taxonomy the reader does not own

**Surface.** `src/components/roadmap/kind-pill.tsx`. Eleven kinds — `cycle, post, asset, press, paid, launch, kpi, milestone, action, blocker, refusal` — each with their own colored chip. Rendered on every item row in the public list via `src/components/roadmap/item-row.tsx` line 75 (`<KindPill kind={task.kind} size="sm" />`).

**Friction.** "Post", "Asset", "Press", "Paid", "KPI" are marketing-team vocabulary. A wedding planner's mother reading her daughter's roadmap sees a small uppercase amber pill that says PAID next to a deposit item and reads it as "paid for" — not as a content-marketing channel category. KPI in `--roadmap-emerald-bg` is the most explicit case: it is dashboard register lifted directly. Eleven categories is the kind of axis a Jira board carries, not the kind of axis a plain-English page anyone can read carries.

Note that R·20 already renamed the pill from `TYPE` to `KIND` (commit b948e2d, "calm-attention layer + KIND pill rename"). The rename is doing real work; the *existence* of the pill on the public surface is the next move.

**The cut.**
- Hide `KindPill` on the public Overview row. Keep `milestone` and `launch` differentiation (those are first-class to the timeline story), but render them as a typographic treatment on the title — not a category chip.
- Move the eleven-kind taxonomy to the editor surface where it belongs.
- If `blocker` needs visibility on the public surface, it already has the `Blockers` card grid at lines 931–955 — that is the right placement.

**Layer.** 2 (Recommend). Hide on public, keep on editor.

---

### Gap 4 · Refusals page header is itself a refusal of plain English

**Surface.** `src/app/[workspaceSlug]/refusals/page.tsx` lines 76–104. The reader-facing structure:

```
Breadcrumb: {workspace.name} / Refusals
MetaStrip: {workspace.name} · REFUSALS · N decisions
H1: "What we said no to."
Sub: "Decisions are only legible if you can see the no's."
```

**Friction.** "Refusals" is the word the operator uses internally. The H1 — "What we said no to." — is on-brand and reads. But the chrome surrounding it — breadcrumb with "Refusals", uppercase MetaStrip with "REFUSALS · N DECISIONS", `<title>` "Refusals · {name} — Roadmap" — uses the internal word three times before the reader gets to the plain-English line. The sub-copy "Decisions are only legible if you can see the no's" is a brand sentence written to the operator about the brand, not to the visitor about the work.

Also: `WorkspaceHeader` line 71 shows a "Refusals" nav chip with the count badge. The word "Refusals" is the operator's word. The reader's word — used elsewhere on this same product — is **"What didn't make it"** (already live in the right rail at `src/app/[workspaceSlug]/page.tsx` line 1139).

**The cut.**
- Rename the public surface: `Refusals` → `What didn't make it`. One word, one source of truth, across nav chip (`workspace-header.tsx` lines 64–80), breadcrumb (`refusals/page.tsx` line 85), MetaStrip (line 91), and page `<title>` (line 29).
- Drop the brand-sentence sub-copy. The H1 carries the meaning. Add nothing.
- The MetaStrip on this surface is pure decoration — there is no timeline or count rhythm to anchor. Cut it.

**Layer.** 2 (Recommend). One rename, one sub-copy cut, one strip removal.

---

### Gap 5 · The marketing hero rewards a visitor the page is not for

**Surface.** `src/app/page.tsx` lines 17–90. The signed-out reader the mandate names — the one who got a link to someone's plan — never sees this page. They land on `/{workspaceSlug}`. The marketing homepage is for *prospects considering signing up to publish their own*.

The problem: the right rail of every public workspace already includes a "Shared update" link, a "What didn't make it" link, and the `WorkspaceHeader` carries `SuiteLauncher` cross-product nav. None of this is wrong. But there is no path back to "what is this thing I'm looking at?" for the recipient who is curious about Signal Roadmap after reading their friend's wedding plan.

The closest path is the suite launcher click → studio umbrella → product card → marketing homepage. Three hops. The marketing homepage when they arrive opens with a hero that reads `Signal Roadmap · Direction clarity` (mono eyebrow) → "The plan your client can actually read." (line 41). The word "client" routes the reader to a service-provider register; the wedding planner's mother is not a client and does not have one. Then the green `animate-pulse` dot reads "Reading-room is live · Publishing reopens soon" (line 61) — a status the recipient cannot act on and is not the question they came to ask.

**Friction.** The recipient who clicks through from a shared plan lands on a page written for someone trying to *buy* the product, not someone trying to *understand* the page they just read. There is no "what is this?" landing for the actual reader the mandate targets.

**The cut.**
- The hero copy should read to both audiences in the same sentence, or split. Current copy reads to one. Replace "The plan your client can actually read." with a line that reads to the recipient first and the operator second.
- Drop the `Reading-room is live · Publishing reopens soon` status pulse. It is status the recipient cannot act on; for the prospect it reads as "the thing I want to do is closed." Both audiences are worse off for its presence.
- Consider a one-line "You arrived here from someone's plan?" link near the footer of the workspace page itself. Direct path back, no marketing detour.

**Layer.** 3 (Founder-gate). Hero copy on the marketing homepage is brand-strategic; route changes to the recipient-arriving path are GTM-strategic. Brief.

---

## Stop doing this

**Stop building parallel views of the same data on the public surface.** Four views (Overview, Roadmap, Milestones, Schedule) is the failure mode the brand calls out by name (BRAND.md §2.2: "Three sidebars, four views, a settings panel with twelve sections. The 80% read this as 'this is not for me.'"). Roadmap is currently shipping four views to a reader who came for one. Every cycle that adds a fifth view (Gantt, Kanban, Calendar, Quarter) is a cycle that betrays the mandate. The discipline is to ship the single plain-English page and refuse the rest. The competitive answer to "but other tools have Kanban" is in BRAND.md §2.3 — refusal discipline sustained across the suite is the moat. The view switcher is the most visible drift on the surface right now.

---

## What is working — for the record

- `DraftNotPublishedContent` (page.tsx lines 1471–1496) is exactly the calm read the mandate asks for. No login wall. No error framing. The plain-English line "This plan isn't public yet. The owner will share it when it's ready." is the brand on a quiet page. Hold the line.
- The `AddressBarChip` (`src/components/marketing/address-bar-chip.tsx`) and R·3's `Copy-link button` on the workspace public URL close the value moment cleanly. The URL *is* the product.
- The Long Now Strip and `RoadmapHeroLoader` on the marketing homepage are the brand gesture (track being laid), and they earn their pixels.
- Status enum `blocked → waiting` (R·21, commit ae52bf4) — the rename is the right read. "Waiting" is what the recipient understands; "Blocked" is what the project manager says. The product is calmer because of the change.

---

_Filed by the Director of Roadmap Product Excellence. Read-only. No commits, no merges. Brief Ethan on Gaps 2 and 5; the rest are Layer 2 cuts the team can run._
